{
  "name": "viz",
  "version": "0.1.0",
  "description": "Visualization service",
  "scripts": {
    "build:client": "esbuild client.ts --bundle --outfile=build/client.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "esbuild": "^0.27.3",
    "ts-proto": "^2.11.2"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.11.0"
  }
}
